# Draft 0.5 freeze-review disposition — 2026-09-05

Reviewed merged baseline: `0a7944c6381165531737d084840f59dccac02616`.

This file records the disposition of the latest independent adversarial review that followed PR #6. Draft 0.6 incorporates the accepted fixes.

| Finding | Draft 0.6 disposition |
|---|---|
| PayoutSafe depended on sender ACK persistence that receiver cannot observe | Fixed: receiver durable checkpoint effect is sufficient; sender ACK remains sender replay/prune state only |
| Outer epoch contract-binding grammar not fully normative | Fixed: Section 11 freezes complete counts, sorting, entry encodings and event-set grammar; full preimage/digest vector added |
| Mining/Miningcore rules only present in tooling | Fixed: `coredrp-mining-v1-semantics.md` and `coredrp-miningcore-v1-semantics.md` incorporated normatively |
| ClockStateUpdate malformed/stale/recovery behavior incomplete | Fixed: exact validity matrix, stream-local generation reset, evidence caps, bound checks and BAD->RECOVERING latch |
| Core/Profile 1.0 vs 1.1 downgrade ambiguity | Fixed: Draft 0.6 endpoints are Core 1.1 only; explicit compatibility registry |
| Permanent financial idempotency unbounded | Fixed: policy v3 bounded active producer generation plus permanent compact retired-generation high-water |
| Temporal policy retroactivity insufficiently blocked | Fixed: ordinary effective time must be beyond payout frontier + uncertainty; retroactive changes require dedicated reconciliation |
| Gap waiver vs PayoutSafe ambiguous | Fixed: unresolved blocks; reconciled restores proof; waived remains permanently non-PayoutSafe and only supports explicit override |
| Bitcoin network validation policy not contract-bound | Fixed: canonical network-policy digest bound into Miningcore semantic contract |
| ADMIN durable idempotency partly implicit | Fixed: atomic lookup/replay/version/mutation/audit/result transaction and evaluation order frozen |
| Semantic retry threshold described as configurable but not bound | Fixed: Mining 1.1 Draft 0.6 fixes threshold to exactly 3; changing it requires profile revision |
| Formal exceptional transition split across model actions | Fixed: model exceptional transition creates gap and retires epoch atomically; unsafe no-gap mutation required to fail |
| Wire fingerprint relied only on Git SHA-1 | Fixed: CI requires both Git blob identity and independent raw-file SHA-256 |
| Metrics registry still Draft 0.4 and lacked idempotency-storage metrics | Fixed: Draft 0.6 registry plus generation records/bytes/high-water and waived/reconciliation metrics |

Draft 0.6 remains pre-implementation. The intended next review target is implementation-freeze readiness and then the Miningcore reference implementation rather than another architecture redesign.
