# CoreDRP/1 specification

The canonical normative specification for the current pre-implementation freeze candidate is:

**[`CoreDRP-1-SPEC-0.6.md`](CoreDRP-1-SPEC-0.6.md)**

This unversioned file is intentionally a pointer and MUST NOT contain a second copy of normative protocol text. CI verifies that the canonical versioned specification and its incorporated registries satisfy the repository integrity gates.

Historical drafts and historical conformance artefacts are non-normative unless the canonical specification explicitly incorporates them.
